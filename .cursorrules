# Instructions

During your interaction with the user, if you find anything reusable in this project (e.g. version of a library, model name), especially about a fix to a mistake you made or a correction you received, you should take note in the `Lessons` section in the `.cursorrules` file so you will not make the same mistake again. 

You should also use the `.cursorrules` file as a Scratchpad to organize your thoughts. Especially when you receive a new task, you should first review the content of the Scratchpad, clear old different task if necessary, first explain the task, and plan the steps you need to take to complete the task. You can use todo markers to indicate the progress, e.g.
[X] Task 1
[ ] Task 2

Also update the progress of the task in the Scratchpad when you finish a subtask.
Especially when you finished a milestone, it will help to improve your depth of task accomplishment to use the Scratchpad to reflect and plan.
The goal is to help you maintain a big picture as well as the progress of the task. Always refer to the Scratchpad when you plan the next step.

# Tools

Note all the tools are in python3. So in the case you need to do batch processing, you can always consult the python files and write your own script.

[NOTE TO CURSOR: Since no API key is configured, please ignore both the Screenshot Verification and LLM sections below.]
[NOTE TO USER: If you have configured or plan to configure an API key in the future, simply delete these two notice lines to enable these features.]

## Screenshot Verification

The screenshot verification workflow allows you to capture screenshots of web pages and verify their appearance using LLMs. The following tools are available:

1. Screenshot Capture:
```bash
venv/bin/python3 tools/screenshot_utils.py URL [--output OUTPUT] [--width WIDTH] [--height HEIGHT]
```

2. LLM Verification with Images:
```bash
venv/bin/python3 tools/llm_api.py --prompt "Your verification question" --provider {openai|anthropic} --image path/to/screenshot.png
```

Example workflow:
```python
from screenshot_utils import take_screenshot_sync
from llm_api import query_llm

# Take a screenshot

screenshot_path = take_screenshot_sync('https://example.com', 'screenshot.png')

# Verify with LLM

response = query_llm(
    "What is the background color and title of this webpage?",
    provider="openai",  # or "anthropic"
    image_path=screenshot_path
)
print(response)
```

## LLM

You always have an LLM at your side to help you with the task. For simple tasks, you could invoke the LLM by running the following command:
```
venv/bin/python3 ./tools/llm_api.py --prompt "What is the capital of France?" --provider "anthropic"
```

The LLM API supports multiple providers:
- OpenAI (default, model: gpt-4o)
- Azure OpenAI (model: configured via AZURE_OPENAI_MODEL_DEPLOYMENT in .env file, defaults to gpt-4o-ms)
- DeepSeek (model: deepseek-chat)
- Anthropic (model: claude-3-sonnet-20240229)
- Gemini (model: gemini-pro)
- Local LLM (model: Qwen/Qwen2.5-32B-Instruct-AWQ)

But usually it's a better idea to check the content of the file and use the APIs in the `tools/llm_api.py` file to invoke the LLM if needed.

## Web browser

You could use the `tools/web_scraper.py` file to scrape the web.
```bash
venv/bin/python3 ./tools/web_scraper.py --max-concurrent 3 URL1 URL2 URL3
```
This will output the content of the web pages.

## Search engine

You could use the `tools/search_engine.py` file to search the web.
```bash
venv/bin/python3 ./tools/search_engine.py "your search keywords"
```
This will output the search results in the following format:
```
URL: https://example.com
Title: This is the title of the search result
Snippet: This is a snippet of the search result
```
If needed, you can further use the `web_scraper.py` file to scrape the web page content.

# Lessons

## User Specified Lessons

- You have a python venv in ./venv. Always use (activate) it when doing python development. First, to check whether 'uv' is available, use `which uv`. If that's the case, first activate the venv, and then use `uv pip install` to install packages. Otherwise, fall back to `pip`.
- 数据库相关配置放在项目根 .env（已 gitignore）：SUPABASE_PROJECT_REF（项目 ID）、SUPABASE_DB_PASSWORD。迁移/修复脚本会从 .env 自动读取。参考 .env.example。
- **数据库迁移（必做）**：每次新增或修改数据库表/列/SQL 迁移脚本时，**必须在本轮对话中立即执行**。执行方式：`cd propertyassistance && python3 web/run-migration-xxx.py`（需在 .env 中配置 SUPABASE_PROJECT_REF、SUPABASE_DB_PASSWORD）。否则会出现 "Could not find the column in schema cache" 等错误。
- **使用前需执行**：当文档（如 README、SUPABASE_SETUP、注释等）中出现「使用前需执行 XXX」「需先执行 XXX」「before use run XXX」等类似说明时，应**主动帮用户执行**该操作（如数据库迁移、脚本、命令等），而非仅提示用户手动执行。
- Include info useful for debugging in the program output.
- Read the file before you try to edit it.
- Due to Cursor's limit, when you use `git` and `gh` and need to submit a multiline commit message, first write the message in a file, and then use `git commit -F <filename>` or similar command to commit. And then remove the file. Include "[Cursor] " in the commit message and PR title.

## Cursor learned

- **CORS**：`allow_credentials=True` 与 `allow_origins=["*"]` 不能同时使用，否则 preflight 会失败。抓取 API 不需 cookie 时用 `allow_credentials=False`；若需 credentials 则改为显式 origins 如 `["http://localhost:5173","https://xxx.vercel.app"]`
- For search results, ensure proper handling of different character encodings (UTF-8) for international queries
- Add debug information to stderr while keeping the main output clean in stdout for better pipeline integration
- When using seaborn styles in matplotlib, use 'seaborn-v0_8' instead of 'seaborn' as the style name due to recent seaborn version changes
- Use 'gpt-4o' as the model name for OpenAI's GPT-4 with vision capabilities

# Scratchpad
## 已完成：ClientView 房源模式 + 联系人号码仅中介可见
- [X] 当 client 是房源（group_type=listing）时：顶部展示房源信息，下方用 table 形式展示预约（复刻中介界面的表格）
- [X] 联系人号码（customer_phone、listing_agent_phone）：仅中介登录时显示「展示联系人号码」按钮，点开后才展示；非中介无按钮、不显示任何号码
- [X]  migration 048：get_client_view 返回 group_type、property_id、appointment 的 customer_info/customer_phone/party_role

## 已完成：UI 风格升级 - Skeleton / Glassmorphism / Aurora / 微交互
- [X] Skeleton Screen：PageSkeleton、FullPageSkeleton、ViewPageSkeleton；应用到 App 路由守卫、ClientView、AgentPublicPage、AdminPage、MapViewModal、AgentFeedback
- [X] 浮窗 Glassmorphism：glass-overlay、glass-overlay-dark、glass-modal、glass-header；所有 Modal、Lightbox 使用
- [X] 官网 Aurora Gradient 变体：Hero 使用 aurora-gradient 动画渐变
- [X] Reveal on scroll（IntersectionObserver）：HomePage 各 section 和卡片
- [X] Micro-interaction 微交互：micro-btn（点击 scale）、micro-hover（悬停抬升+阴影）、micro-scale（悬停放大）；应用到 CTA、卡片、Modal 按钮、Lightbox

## 已完成：代表卖家/房东的合并预约卡片 + Playground
- [X] 同一房源、同一日期、多组潜在买家且无穿插时合并为一个大卡片
- [X] 多种时间表布局（表格/时间轴/紧凑行/分块卡片/极简）在 /playground 可选
- [X] 选择后保存到 localStorage，AgentDashboard 预约列表生效
- [X] 合并逻辑：lib/sellerMergedBlocks.ts；组件：SellerLandlordMergedCard.tsx

## 已完成：代表卖家/房东的预约卡片优化
- [X] 拆分两种卡片：代表买家/租客沿用完整卡片；代表卖家/房东使用精简卡片
- [X] 精简卖家/房东卡片：仅展示房源名、预约时间、备注、潜在买家联系方式、WhatsApp、编辑删除刷新
- [X] 移除卖家/房东卡片的图片、价格、卧室浴室面积、户型图、查看房源、卖家中介信息
- [X] 视觉区分：amber 左边框、#fffbeb 背景、顶部醒目「代表卖家/房东」徽章

## 已完成：刷新按钮改为异步抓取
- [X] 将 AgentDashboard 中 handleRefreshProperty 改为调用 triggerScrapeAsync（即时返回）+ 展示 asyncScrapeHint
- [X] 两个 handleRefreshProperty（PendingAppointmentsSection、AppointmentsSectionContent）均已修改

## 已完成：时间表卖家预约合并修复
- [X] 新增 mergeConsecutiveSellerBlocksByStartGap：连续卖家/房东块且起始时间差 < 45 分钟则合并，不显示休息

## 已完成：时间表自定义事件
- [X] 添加事件按钮 + 弹窗（名称、日期、起止时间、地点）
- [X] 自定义事件用 violet 特殊颜色区分，支持删除
- [X] localStorage 存储，按 user.id 隔离

## 已完成：中介时间表 - 替换客户视图，新增智能合并的时间表
- [X] 删除「客户视图」tab
- [X] 新增「时间表」tab：展示未来预约，同客户同天相邻时段（30分钟内）合并为一块
- [X] 支持「未来两周」/「全部」切换，按天卡片展示，每块显示客户名、时间段、看几套房
- [X] Backend: 抓取 Property Guru 房源的 listing_agent_name、listing_agent_phone
- [X] 数据库: migrations/010_listing_agent_fields.sql 添加字段
- [X] Frontend: 预约卡片显示「卖家中介」及 WhatsApp 按钮